# Sessions — workspace//kit

Append-only log of who touched this workspace, in what tool, and what state it was left in. One entry per session, added by whichever agent/tool is ending its session — this is a mechanical handoff record, not project history.

**Boundary with CHANGELOG.md** (resolved 2026-07-27, see DECISIONS.md): CHANGELOG.md — once created, see docs/specs/context-manager-conventions.md → #34 — is narrative *product* history (what shipped, what was decided; coarse-grained, judgment-driven, infrequent). This file is a *tool-handoff* record (who touched the workspace, in what tool, leaving it in what state); written far more mechanically and far more often, every session, with no editorial judgment involved. An entry here may reference a decision or issue (e.g. "see DECISIONS.md, 2026-07-26") but never restates the *why* — that always lives in DECISIONS.md/CHANGELOG.md, not here.

## Entries

### 2026-07-27 — Cowork
**Did:** Resolved the CHANGELOG.md vs. SESSIONS.md boundary (orthogonal axes, see DECISIONS.md); created this file; populated `queue/` with the two real Workspace//Kit voice-note references; refreshed docs/PRD.md and PROJECT.md to reflect the 5 specs written 2026-07-26 (workspace inspection layer #77, health check #78, git integration layer #79, document ingestion queue #80, cross-tool session log #81) and the "kit" rename; added standing "every session" / queue instructions to AGENTS.md.
**Left at:** No code touched — planning/docs only. Repo is ready for Claude Code to start implementing the specs. Next: prototype the new structures (workspace inspection/health-check output, queue, session log) in Claude Design before implementation begins, per the owner's request.

### 2026-07-28 — Cowork
**Did:** Reviewed the Claude Design prototype ("Workspace Kit Dashboard") against the specs; wrote `docs/design/git-layer-dashboard-brief.md`, which the prototype then implemented (Source control section, history-growth rows, broken-reference badges, token-estimate labels). Adapted the plan to it: new spec `docs/specs/web-app-dashboard.md` (rendering-only bridge between the prototype and #77–#81), Web App stack + visual identity decided (React/Tailwind/shadcn, scoped per surface — see DECISIONS.md, 2026-07-28), AGENTS.md's build-framework limit rescoped, `local-web-app.md`'s design Non-Goal retired and its "open an existing workspace" bullet superseded, prototype markup versioned at `docs/design/workspace-kit-dashboard.dc.html` with the full export gitignored.
**Left at:** Changes are in the working tree, uncommitted — local `main` is 3 commits behind `origin/main` (earlier work this session was pushed via the GitHub API while local git had no credentials). Claude Code was working in the `tasks-progress` worktree in parallel; `DESIGN.md` and `docs/design/prototype-prp.md` live there uncommitted. Needs a `git pull` before committing.

### 2026-07-29 — Claude Code (background job, `tasks-progress` worktree)
**Did:** Executed the "project type" → **kit** rename across both copies of the generation logic, the parity harness/fixtures, `research/kits/`, and README/CONTEXT/specs wording (see DECISIONS.md, 2026-07-29, including the boundary that generated text keeps saying "Project type"). Re-mirrored `index.html` from `src/workspace-kit.html`, which had been a version behind since 2026-07-27.
**Left at:** Branch `worktree-tasks-progress` pushed with a draft PR; `npm run test:fixtures` and `npm run test:parity` both pass. The branch also still carries four earlier unpushed commits from the 2026-07-27 session (CONTEXT.md rename propagation, non-functional bar, PT starter prompt, `DESIGN.md` + prototype PRP) — the PR lands all of them. **Not touched:** the 2026-07-28/29 Local Web App batch (`docs/specs/web-app-dashboard.md`, `docs/design/workspace-kit-dashboard.dc.html`, the DECISIONS.md 2026-07-28 entry, the AGENTS.md build-framework rescope) was being actively written in the main checkout during this session and is still uncommitted there — it needs its own commit and will conflict at the tail of DECISIONS.md/TASKS.md with this PR.

### 2026-07-29 — Claude Code (background job, `webapp-plan` branch)
**Did:** Ported the 2026-07-28 Local Web App batch off the main checkout's uncommitted working tree onto `main`: the DECISIONS.md 2026-07-28 stack/identity entry (inserted chronologically, before the 2026-07-29 rename entry), AGENTS.md's build-framework limit rescoped per surface, `.gitignore` rules for the Claude Design exports, `docs/specs/web-app-dashboard.md`, the prototype markup at `docs/design/workspace-kit-dashboard.dc.html`, `docs/specs/local-web-app.md`'s retired/superseded bullets, and the TASKS.md dashboard section. `DESIGN.md`'s three Open Questions marked resolved, which the 2026-07-28 entry had listed as pending on that file reaching `main`.
**Left at:** `main` now carries both the kit rename (#165) and this batch. The main checkout at `/workspace` is still on the pre-merge commit with the now-redundant working-tree changes — its local `main` (9f38499) has the same tree as `origin`'s `8ae0361`, so it needs `git reset --hard origin/main` (nothing in its working tree is unique any more) rather than a merge. Not done from here: that checkout belongs to a live session.

### 2026-07-29 — Claude Code (background job, tracker cleanup)
**Did:** Audited the repo for pending work before a change of direction: no uncommitted changes, no stashes, no unmerged branch content (the five stale local branches and the `tasks-progress` worktree all had their content already on `main`; removed). Then pruned the issue tracker from 168 open to 90 — see DECISIONS.md, 2026-07-29 — and rewrote the `Sync specs to Issues` workflow to match by stable sync key instead of exact title, with the logic moved to `.github/scripts/sync_specs_to_issues.py` (`--dry-run` supported). Stamped #126 with its sync key by hand, since its title predates the kit rename and no longer matches its bullet.
**Left at:** `main` clean and synced. The 90 open issues are 11 `[Spec]` parents plus their P0 sub-issues, with the pre-spec backlog that is still live (#5, #7, #8, #9, #12, #14, #21–#31) deliberately left open for triage under the new direction. **Blocked:** the workflow fix is committed on branch `worktree-workflow-sync-fix` but cannot be pushed — the OAuth token lacks the `workflow` scope, so it cannot modify `.github/workflows/`. Run `gh auth refresh -s workflow`, then push that branch and open its PR. Until it lands, the old title-matching workflow is still what runs on a push to `docs/specs/*.md`, and it will keep creating duplicates when a P0 bullet is reworded.

### 2026-07-29 — Claude Code (background job, `inspect-layer` worktree)
**Did:** Implemented the workspace inspection layer (docs/specs/workspace-inspection-layer.md → #77) as `core/inspect.js`, the read-side counterpart to `core/generator.js`: file discovery with per-file byte/line/char/token metrics, agent-layer instruction parsing into stable-ID units, and the cross-reference graph with unresolved edges flagged. Test suite `npm run test:inspect` (18 cases, one per P0 acceptance criterion); `npm test` now runs all three suites. Closed three of the spec's Open Questions in the process — token heuristic, instruction granularity, Node-only export — plus two decisions the spec did not anticipate (gitignore-aware scanning, no edges from inline-code syntax examples). All recorded in DECISIONS.md, 2026-07-29.
**Left at:** `#77`'s P0 done and two of its P1s; the module is unused so far — nothing consumes it yet. Next consumer in line is the workspace health check (#78), which was explicitly blocked on this and whose own P0 already assumes the chars ÷ 4 estimate this module implements. The git integration layer (#79) and the dashboard (#169) are likewise unblocked. Note for whoever takes #78: `inspect()` deliberately computes no verdicts and applies no thresholds — that judgement is #78's to own, and the boundary is worth keeping sharp.

### 2026-07-29 — Claude Code (background job, `doctor` branch)
**Did:** Implemented the workspace health check (docs/specs/workspace-health-check.md → #78) as `core/doctor.js` plus a `workspace-kit doctor` command in the new `bin/workspace-kit.js`: always-loaded context budget, per-file banding against #34's ~300-line anchor, decision-log growth against the ~15-entry rotation trigger, broken cross-references, stale instructions, and concrete per-finding suggestions. `--json` and configurable thresholds included. `npm test` now runs four suites. Confirmed with the owner that the Local Web App reaches the filesystem through a **local Node server**, closing the question that gated #169 (see DECISIONS.md).
**Left at:** Ran the doctor on this repo and acted on what it said — it reported `DECISIONS.md` at 17/15 entries, so the second ADR rotation was performed (entries 009–010 moved to `decisions/`), taking the repo back to exit 0. Cross-referencing the Claude Design prototype's 238 `{{ }}` placeholders against the specs also surfaced a gap: #80 and #81 both delegate *reading* their own files to #77, but #77's Non-Goals never claimed it — so `parseSessions()`/`parseQueue()` belong in `core/inspect.js` and are the next thing to build, before #79 and the dashboard.

### 2026-07-29 — Claude Code (background job, `readers` branch)
**Did:** Closed the ownership hole the prototype cross-reference exposed: `core/inspect.js` now parses `queue/*.md` into `index.queue` and `SESSIONS.md` into `index.sessions`, so #80 and #81 do not each grow their own parser (see DECISIONS.md, 2026-07-29). Both specs gained an "Implementation status" section separating the finished read half from the still-open generation half.
**Left at:** Three of the five data sources the dashboard (#169) needs are now available from `core/`: Overview and the graph (#77), Health check (#78), Queue and Session log (this). The remaining one is git state (#79), which is unblocked. **Not done and deliberately so:** the generation half of #80/#81 — emitting `SESSIONS.md`, the standing instruction and the `queue/` convention into generated workspaces. That changes generated output on all three surfaces, so it needs the fixtures re-recorded and a manual backport into `src/workspace-kit.html`'s inline copy, which CLAUDE.md asks to be reviewed rather than applied unattended.

### 2026-07-29 — Claude Code (background job, `gitlayer` branch)
**Did:** Built the unblocked half of the git integration layer (#79) as `core/git.js` + `workspace-kit status`: file-state tracking joined onto the inspection index, safe-edit checks, plain-language change summary, templated commit message, and both guardrail tests (no outbound AI call; HTML artifact untouched). 11 cases in `npm run test:git`; `npm test` now runs five suites.
**Left at:** All five of the dashboard's (#169) data sources now exist in `core/` except the PR/worktree flows, which are **blocked on two owner decisions** recorded in TASKS.md and DECISIONS.md: hosting-provider scope for PRs, and worktree placement/naming. Next unblocked work is the dashboard itself — the local Node server was confirmed 2026-07-29 — but it should render against the four finished sources and leave the Source control section's action buttons inert until those two answers land.

### 2026-07-29 — Claude Code (background job, `server` branch)
**Did:** Built `core/server.js` + `workspace-kit serve`, the read-only local JSON surface the dashboard consumes, acting on the local-Node-server decision confirmed earlier the same day. `/api/dashboard` returns all five prototype sections from one scan. Zero-dependency, localhost-only, path-escape guarded, non-GET refused — 9 cases in `npm run test:server`; `npm test` now runs six suites. Verified end-to-end against this repo over HTTP: 72 files, 263 graph edges, health verdict, 5 session entries, 2 queue items, git branch and change list.
**Left at:** The dashboard's entire data layer is done — every number docs/specs/web-app-dashboard.md renders is now served. **The remaining work is the front end itself** (React + Tailwind + shadcn per DECISIONS.md 2026-07-28), converting `docs/design/workspace-kit-dashboard.dc.html` from its Claude Design export form. Two things stay blocked on owner decisions and should not be built ahead of them: the PR flow's hosting-provider scope, and worktree placement/naming (#79). The payload's `capabilities` flags exist so the UI can show those controls as unavailable.

### 2026-07-29 — Claude Code (background job, `reports-folder` worktree)
**Did:** Verified the state of the 2026-07-29 implementation batch by running it, not reading it — `npm test` (61 cases across all six suites, all pass), all four CLI commands against this repo, and the server over HTTP. Then created `reports/` at the owner's request: generated reports are now written as self-contained HTML there instead of being answered only in chat. Added `reports/README.md` (naming, self-containment, identity and the "say what was actually verified" rule, plus the boundary against SESSIONS/DECISIONS/TASKS), a `## Reports` instruction in AGENTS.md so the convention survives future sessions, and the first report at `reports/2026-07-29-estado-do-projeto.html`.
**Left at:** Branch `worktree-reports-folder` with a draft PR; `npm test` passes and `doctor` reports 0 broken cross-references with AGENTS.md at 40 lines (15% of the 300-line budget). Two real findings the run surfaced, neither fixed here: `.claude/worktrees/` is not in `.gitignore` and `status` already flags it as an accidental commit risk, and `DECISIONS.md` sits at exactly 15/15 entries so the next entry triggers an ADR rotation. Also noted for whoever builds the dashboard: `/api/sessions` and `/api/queue` are not endpoints — both come back as sections inside `/api/dashboard` — which is a design call worth confirming rather than a defect.

### 2026-07-29 — Claude Code (background job, `gitlocal` worktree)
**Did:** Acted on the owner's answers to three of the four blocked decisions. Scoped the git layer to **local git only** — push/PR deferred as their own step, with a test asserting `core/git.js` contains no push/fetch/clone/pull or `gh pr` call so the boundary cannot erode quietly. Implemented **worktree create/list/remove** on the answered convention: `<workspace>/.worktrees/<name>` on a same-named branch by default, `--path`/`--branch` overriding either, the ignore rule added automatically (which also fixes the stray-worktree noise `status` had flagged in this repo), removal refusing uncommitted work unless forced and always keeping the branch. `npm run test:git` 11 → 22 cases, including the spec's two-worktree parallel-edit criterion tested directly rather than by proxy. Recorded all three decisions in DECISIONS.md, closed the spec's Open Questions, and rotated three entries to `decisions/` when the doctor asked (18/15 → 15/15).
**Left at:** Branch `worktree-gitlocal` with a draft PR; `npm test` 72 cases across six suites, all pass; doctor reports 0 broken cross-references. **Correction on the record:** the previous session's entry said the spec→issue workflow fix was blocked on the OAuth `workflow` scope. It was not — that fix had already landed on `main` in 562e78a via PR #183. The claim was carried into the 2026-07-29 report unverified and has been corrected there. **Still blocked, deliberately:** the Markdown Orchestrator integration — new context from the owner is that it is a Tauri app with a predominantly web front end, so the question is as much about runtime/packaging as code reuse; it gets its own branch later. **No longer blocked:** the shared instruction library, which was wrongly parked as contradicting the Templates Non-Goal — generation and management are separate phases and that Non-Goal only constrains generation (see DECISIONS.md). It needs a spec, not a decision. Next unblocked work of size remains the dashboard front end (#169).

### 2026-07-29 — Cowork
**Did:** Built the report layer: `core/report.js` (zero-dependency Markdown-subset renderer + page builders) rendering the repo's own planning documents into `reports/` — a dashboard index, `reports/plans/` (PRD, 11 specs, design docs, PROJECT/CONTEXT/DESIGN/AGENTS/CLAUDE), a decisions timeline (DECISIONS.md + the archived `decisions/` ADRs), a task board off TASKS.md and the session log. Wired as `workspace-kit report [dir]` / `npm run report`, tested by `core/check-report.js` (11 cases, added to `npm test` as the seventh suite). Chrome is in Portuguese, document content untouched; visual direction follows DESIGN.md (neutral base, amber/teal, Inter + JetBrains Mono, dark/light equally first-class). TASKS.md and AGENTS.md updated with the feature and the `reports/` convention.
**Left at:** Uncommitted in the working tree. `npm run test:report` passes and all 24 generated pages parse strictly as HTML5 with zero broken links. **Not decided:** whether `reports/` stays versioned or becomes gitignored build output, and whether a `reports/health.html` (doctor/status output) should exist given it overlaps the Web App dashboard (#169) — both logged as open bullets under TASKS.md's "Report layer" section. No spec written: the feature was requested directly, not ingested from a voice note.

### 2026-07-29 — Claude Code (background job, `dashboard` branch)
**Did:** Started the Local Web App dashboard front end (#169) after confirming the planning already existed — `docs/specs/web-app-dashboard.md` had P0 acceptance criteria per section, so this was implementation, not planning. Reorganised first, on the owner's call: created the long-lived `dashboard` branch, consolidated the unmerged `worktree-gitlocal` and `worktree-reports-folder` work into it, and brought Cowork's report layer (`core/report.js`, the generated `reports/` pages, the `report` command, the seventh suite) across from the main checkout's working tree — four files needed a real merge rather than a copy. Nothing goes to `main` from here until the feature is done. Then built `web/`: Vite + React 19 + Tailwind v4 in its own package, all five sections live against `/api/dashboard`, dark/light, the not-a-workspace state, and 12 render tests each mapping to a P0 criterion. Added the worktree list to the server payload, which the Source control section needed and which the server predated.
**Left at:** Branch `dashboard`, not pushed anywhere and no PR — `main` untouched at `97a943d`. 84 core tests across seven suites plus 12 dashboard tests, all passing; doctor exit 0 with 0 broken cross-references; DECISIONS.md rotated 17/15 → 15/15. Verified end to end, not just built: the compiled app serves over HTTP and `/api/dashboard` proxies through it returning 166 KB of this repo's real data. **To run it:** `node bin/workspace-kit.js serve .` then `npm --prefix web run dev`. **Deliberately renders rather than executes** — every capability flag is false and blocked actions say why, since the server is read-only and the git layer local-only. **Still open in the spec:** where suggestion dismissals and the workspace list persist (theme is in localStorage; the other two are not persisted rather than guessed). **Not built (P1):** command palette, workspace switcher, dismissals, worktree conflict flag, and serving the compiled dashboard from `workspace-kit serve` so it runs without Vite.

**Branch consolidation done at the owner's request:** `dashboard` pushed to origin; `worktree-gitlocal` and `worktree-reports-folder` deleted locally and on the remote after confirming with `git branch --merged` that both are fully contained in `dashboard` — which auto-closed PRs #190 and #191. Six other stale local branches deleted on the same check. `main` is untouched at `97a943d` and nothing was merged into it.

**⚠ Open, needs an owner decision before work resumes — a second dashboard exists.** Branch `worktree-dashboard` (commit `efc2afd`, 2026-07-29 05:30, checked out at `.claude/worktrees/inspect-layer`) carries *another* complete P0 implementation of this same spec, built in parallel by another session: 25 files, 3686 lines, in `app/` rather than `web/`. It was **not** deleted and has been pushed to origin so it cannot be lost. The two converge architecturally — own package.json, pure client of `core/server.js`, capabilities-driven disabled controls, one Status component pairing colour with icon and label — and differ in verification: `app/` asserts *rendered* values equal the API's through a real browser via Playwright (14 checks); `web/` asserts the same boundary through server-rendered markup without a browser (12 checks). `app/` also pulls lucide-react/clsx/cva/tailwind-merge and Tailwind 3 + React 18; `web/` vendors its primitives on Tailwind 4 + React 19. Only one should survive. Whoever resumes: do not merge both.

### 2026-07-30 — Claude Code (background job, `dashboard` branch)
**Did:** Answered the owner's question "how do I test the two architectures" by actually running both, which turned the open decision from a paper comparison into an evidenced one. Set up a side-by-side harness: one `core/server.js` feeds both front ends (the current payload is a superset of the one `app/` was built against, so no adaptation was needed), driven by the throwaway `bin/compare-dashboards.sh`. Drove all five sections of each in a real browser via Playwright. Two defects surfaced and were fixed in `web/`: (1) **it was broken in the browser — white screen** — `App.jsx` rendered `health.method` as text when the server returns an object, aborting the React tree; the 12 render tests missed it because they render sections, never the shell; (2) the two implementations' **ports were crossed** (`web/` 4320/4321 dev/preview vs `app/` 4321/4320), so one silently answered for the other — an orphaned `vite preview` from `app/`'s `check:ui` produced a full round of screenshots of `app/` labelled `web/`. `web/` moved to 4330/4331 with `strictPort` and an explicit `127.0.0.1` bind. Wrote `reports/2026-07-30-comparativo-dashboards.html` — how to run both, what running them exposed, ten screenshots embedded as data URIs (the folder's self-contained rule), and a recommendation.
**Left at:** Both implementations verified running against live data with zero JS errors across all five sections. Suites: 84 core, 12 `web/`, 19 `app/` Playwright — all pass; doctor exit 0. **The decision is still the owner's and is still open.** Recommendation recorded in the report: **keep `web/`, port `app/`'s Playwright harness into it** — `web/` covers more P0 surface today (safe-edit warning, worktree list, current blocked-reason copy) because it tracks the current `core/`, while `app/`'s Source control still cites two product decisions that were resolved 2026-07-29; but `app/`'s browser-level verification is the better method and the white-screen bug is the proof. **Cleanup owed once decided:** delete `bin/compare-dashboards.sh`, the losing folder, and its branch — the note in AGENTS.md says so too.

### 2026-07-30 — Claude Code (background job, `dashboard` branch)
**Did:** Executed the owner's decision on the two competing dashboards: **`web/` is the dashboard, `app/` is retired, and `app/`'s Playwright harness came across with it** (see DECISIONS.md, 2026-07-30). Ported `check-ui.mjs` into `web/`, adapted to its UI rather than copied — it builds the app every run, serves the *build* against the real `core/server.js`, and asserts rendered values equal the API's; 21 checks, up from `app/`'s 19. Added the stable `data-*` hooks the harness addresses (`data-nav` + `aria-current`, `data-file`, `data-suggestion`, `data-queue-status`, `data-session`, `data-capability`, `data-theme-toggle`, `data-show-all`) so restyling cannot silently break it. Three real changes fell out of the port rather than being cosmetic: the always-loaded budget is now a real `role="meter"` with `aria-valuenow` (an accessibility gain `app/` had and `web/` lacked); the worktree card gained an explicitly disabled "Manage worktrees" control, so all three `capabilities` flags are drawn unavailable-with-reason instead of two being drawn and one implied; and the Overview completeness check expands the collapsed "other" group first, so it asserts against the API's file count and not a page size. Retired `app/`: worktree `.claude/worktrees/inspect-layer` removed, local branch `worktree-dashboard` deleted, `bin/compare-dashboards.sh` deleted. DECISIONS.md rotated 16/15 → 15/15 (entry 019).
**Left at:** Three suites, all passing and all run: 84 core, 12 `web/` render (`npm run test:web`), 21 Playwright (`npm run test:ui`). Doctor exit 0, 0 broken cross-references. The two dashboard suites are deliberately **not** redundant — the fast one enforces the data-boundary rules on every change, the slow one is the only thing that catches a shell that fails to mount, which is exactly what got past everything on 2026-07-29. **Deliberately not deleted:** `origin/worktree-dashboard` still holds the discarded `app/` implementation at `efc2afd`. Removing a branch from a public remote is the owner's call, not a cleanup step; `git push origin --delete worktree-dashboard` is all that is left. **Open, unchanged:** the owner mentioned preferring `app/`'s slightly darker background but asked for no change — `--background` under `:root[data-theme='dark']` in `web/src/theme.css` (currently `#18181b`) is the single token, and AGENTS.md requires a DECISIONS.md entry before the palette moves. **Still not built (P1):** command palette, workspace switcher, suggestion dismissal, worktree conflict flag, and serving the compiled dashboard from `workspace-kit serve` so it runs without Vite.

### 2026-07-30 — Claude Code (background job, `dashboard` branch)
**Did:** Deleted `origin/worktree-dashboard` after verifying what it held: exactly one unique commit, adding 18 files all under `app/`, plus doc edits describing `app/` itself. The one technically useful thing on it — Vite's default `localhost` resolving to `::1`, where `curl` falls back to IPv4 and Node's `fetch` does not — was already carried into `web/vite.config.js` and `web/check-ui.mjs`, and `reports/2026-07-30-comparativo-dashboards.html` keeps the screenshots. Then answered "when can I test this on Windows" by finding the actual obstacle: **this project develops inside a Docker container** (bridge IP, Docker's embedded DNS, no `/mnt/c`) whose `docker-compose.yml` published no ports at all — its comment still read "if a dev server is added later, map its port here". So `127.0.0.1` inside the container was never reachable from the host, and nothing about WSL or the browser was at fault. Fixed the root cause rather than the symptom: **`workspace-kit serve` now serves the built dashboard itself** from `web/dist` at `/`, alongside the JSON at `/api/*`, so using it is one build and then one command on one port with no Vite in the runtime story. Added `--host` (default unchanged at 127.0.0.1, warns when widened, which a container needs), and published the ports in `docker-compose.yml` to the host's **loopback only**. The path-escape check now covers the asset directory, and non-GET is refused at `/` as well — both tested, including percent-encoded and `assets/../../` traversal.
**Left at:** Three suites, all run and passing: 91 core across seven suites (7 new server cases), 12 `web/` render, 21 Playwright. Doctor exit 0. DECISIONS.md rotated 16/15 → 15/15 (entry 020). Verified end to end in a real browser against `serve` alone on port 4319, no Vite: five nav items, live file rows, section switching, zero JS errors. **Blocked on the owner, and only on this:** the container has to be recreated for the new port mapping to take effect — `docker compose up -d` from the host. Nothing in the repo can do that from inside the container. After that, `npm --prefix web install && npm --prefix web run build`, then `node bin/workspace-kit.js serve . --host 0.0.0.0`, and http://localhost:4319 works from the Windows browser. **Deliberately not done:** packaging it as a desktop app. `serve` plus a browser is enough to use it, and that question overlaps the Markdown Orchestrator's Tauri shell, which the owner deferred to its own branch — logged as an open item in TASKS.md rather than started.

### 2026-07-30 — Claude Code (background job, `dashboard` branch)
**Did:** Audited what a container recreate would destroy before recreating it, at the owner's request. `/workspace` and `/root/.claude` are both 9p bind mounts from `C:\`, so code, git history, worktrees, the two Claude memories, all 9 session transcripts, jobs, plugins and file-history were never at risk. Two things were: **`~/.claude.json`** (43 KB — Claude Code's OAuth account, user id and per-project state, which sits *outside* `~/.claude`) and **`~/.config/gh`** (the GitHub token; the global gitconfig routes git credentials through `gh auth git-credential`, so losing it breaks `git push`, not just `gh`). Backed both up to `.claude_global/_container-backup/` — on the Windows disk, inside a gitignored directory, mode 600 — with a README carrying the exact restore commands. Then made the loss impossible to repeat: `docker-compose.yml` now binds `.claude_global/claude.json` to `/root/.claude.json` and puts `/root/.config/gh`, `/root/.npm` and `/root/.cache` on named volumes, the last mainly so the ~650 MB Playwright browser download survives. Added `CHOKIDAR_USEPOLLING` since Windows bind mounts deliver no inotify events and Vite HMR needs polling. Also confirmed the main checkout's uncommitted working tree at `/workspace` holds nothing unique — `core/report.js` and `core/check-report.js` are byte-identical to `dashboard`, and every line that differs in `AGENTS.md`/`bin/workspace-kit.js`/`package.json` is an *older* version of text `dashboard` has superseded.
**Left at:** Compose YAML validated; ports publish to the host's loopback only. **The owner has to run `docker compose up -d` from Windows** — nothing inside the container can recreate its own container, and the compose file Docker reads is the one at `/workspace` on `main`, not this branch's, so the ports block has to be copied there or the checkout moved to `dashboard` first. **First run after the recreate needs three restores**, all in `.claude_global/_container-backup/README.txt`: the `gh` config (its named volume starts empty), and `npx playwright install chromium` for `npm run test:ui`. `.claude.json` should already be in place via the new bind — the README says how to check. Git commit identity is unaffected: it lives in `/workspace/.git/config`, on the bind mount.

### 2026-07-30 — Claude Code (background job, `dashboard` branch)
**Did:** Adopted named volumes for `node_modules` at the owner's call — `/workspace/node_modules` and `/workspace/web/node_modules` now live on the Linux filesystem instead of crossing the Windows bind mount on every file read, which is the pattern the owner's other project already uses. I had left this out of the previous pass as an unrequested behaviour change; the owner's reasoning (install and build speed, no NTFS round trip) is the better one and the reinstall cost is a one-time thing the volumes then absorb. Two consequences written into the compose file rather than left to be discovered: the volumes start empty, so one `npm install` plus `npm --prefix web install` is needed after the first `up`; and the host can no longer see those folders, so a Windows editor will not resolve imports into them. Worktrees under `.claude/worktrees/` are deliberately not covered — each keeps its own `web/node_modules` on the bind mount, which is fine for something temporary. Also corrected the restore order in `.claude_global/_container-backup/README.txt`: `playwright install chromium` has to come *after* the npm installs, since the playwright package itself lives in `web/node_modules`.
**Left at:** The finished `docker-compose.yml` was written directly into the owner's checkout at `/workspace` — on `main`, at their explicit request, since that is the file Docker actually reads. **It is a working-tree edit only: not committed, not pushed, `main` still at `97a943d`.** The same file is committed on `dashboard`. Both validated as YAML and confirmed byte-identical. Nothing else changed on `main`.
