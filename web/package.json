{
  "name": "workspace-kit-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local Web App dashboard (docs/specs/web-app-dashboard.md → #169). Kept in its own package so core/ stays zero-dependency.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node check-render.js"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "tailwindcss": "^4.1.14",
    "vite": "^7.1.9"
  }
}
